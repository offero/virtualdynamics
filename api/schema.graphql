
type Book {
  title: String
  author: String
  url: String
}

input BookInput {
  title: String!
  author: String!
  url: String!
}

type Query {
  hello: String
  books: [Book]
}

type Mutation {
  addBook(book: BookInput!): Boolean
  deleteBook(title: String): Boolean
}
