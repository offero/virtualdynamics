type Query {
  hello: String
  books: [Book]
}

type Mutation {
  addBook(title: String, author: String, url: String): Boolean
  deleteBook(title: String): String
}

type Book {
  title: String
  author: String
  url: String
}
